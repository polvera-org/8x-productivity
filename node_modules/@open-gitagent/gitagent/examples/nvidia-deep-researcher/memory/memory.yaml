layers:
  working:
    file: MEMORY.md
    max_lines: 200
    format: markdown
  archive:
    directory: archive/
    rotation: monthly
    format: yaml

update_triggers:
  - on_session_end
  - on_explicit_save

archive_policy:
  max_entries: 500
  compress_after_days: 90
  retention_days: 365
