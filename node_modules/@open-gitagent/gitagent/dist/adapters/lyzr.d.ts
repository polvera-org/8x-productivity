export interface LyzrAgentPayload {
    name: string;
    description: string;
    agent_role: string;
    agent_goal: string;
    agent_instructions: string;
    provider_id: string;
    model: string;
    temperature: number;
    top_p: number;
    llm_credential_id: string;
    store_messages: boolean;
    features: object[];
    response_format?: object;
    file_output: boolean;
}
/**
 * Export a gitagent directory to a Lyzr API-ready payload for agent creation.
 */
export declare function exportToLyzr(dir: string): LyzrAgentPayload;
/**
 * String export for the `gitagent export --format lyzr` command.
 */
export declare function exportToLyzrString(dir: string): string;
