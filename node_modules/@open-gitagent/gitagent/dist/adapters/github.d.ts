export interface GitHubModelsPayload {
    model: string;
    messages: Array<{
        role: string;
        content: string;
    }>;
    temperature: number;
    max_tokens: number;
    stream: boolean;
}
/**
 * Export a gitagent directory to a GitHub Models API-ready payload.
 */
export declare function exportToGitHub(dir: string): GitHubModelsPayload;
/**
 * String export for `gitagent export --format github`.
 */
export declare function exportToGitHubString(dir: string): string;
