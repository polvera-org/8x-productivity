import { type ParsedSkill } from './skill-loader.js';
export interface DiscoveredSkill {
    name: string;
    description: string;
    license?: string;
    directory: string;
    source: SkillSource;
}
export type SkillSource = 'agent' | 'agentskills' | 'personal' | 'claude' | 'github';
interface DiscoveryOptions {
    /** Agent directory to search from */
    agentDir: string;
    /** Only return skills from agent-local paths */
    localOnly?: boolean;
}
/**
 * Discover all skills from standard locations.
 * Deduplicates by name: local > agentskills > tool-specific > personal.
 */
export declare function discoverSkills(options: DiscoveryOptions): DiscoveredSkill[];
/**
 * Discover and fully load all skills from standard locations.
 */
export declare function discoverAndLoadSkills(options: DiscoveryOptions): ParsedSkill[];
/**
 * Find a single skill by name from standard locations.
 */
export declare function findSkill(name: string, agentDir: string): DiscoveredSkill | null;
export {};
