export interface ResolveRepoOptions {
    branch?: string;
    refresh?: boolean;
    noCache?: boolean;
}
export interface ResolveRepoResult {
    dir: string;
    cleanup?: () => void;
}
export declare function resolveRepo(url: string, options?: ResolveRepoOptions): ResolveRepoResult;
